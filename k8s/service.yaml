apiVersion: v1
kind: Service
metadata:
  name: springboot-app-service
spec:
  selector:
    app: springboot-app
  ports:
    - protocol: "TCP"
      port: 8085
      targetPort: 8085
      nodePort: 32567
  type: LoadBalancer
